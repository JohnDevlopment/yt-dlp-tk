from yt_dlp_tk.interface import ExText, ExEntry, ExTree
from yt_dlp_tk.yt_funcs.core import extract_video_info
from tkinter import Tk, ttk
import builtins

# root = Tk()
# root.title("Entry")
# root.geometry("700x600")

# frame = ttk.Frame()
# frame.pack(fill='both', expand=True)

# text = ExText(frame, width=50)
# text.pack()

# g = globals()
# for S in ['ExText', 'ExEntry', 'ExTree', 'Tk', 'ttk', 'root', 'text']:
#     setattr(builtins, S, g[S])
